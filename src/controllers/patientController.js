const prisma = require("../config/db");

const getOwnReports = async (req, res) => { try { const reports = await prisma.report.findMany({ where: { patient_id: req.user.id, hospital_id: req.user.hospital_id }, select: { report_id: true, title: true, category: true, notes: true, file_url: true, created_at: true, doctor: { select: { full_name: true } } }, orderBy: { created_at: "desc" } }); res.json({ success: true, reports: reports.map(({ doctor, ...r }) => ({ ...r, doctor_name: doctor?.full_name || null })) }); } catch (e) { console.error(e); res.status(500).json({ success: false, message: "Internal Server Error" }); } };
const getOwnPrescriptions = async (req, res) => { try { const prescriptions = await prisma.prescription.findMany({ where: { patient_id: req.user.id, hospital_id: req.user.hospital_id }, select: { prescription_id: true, diagnosis: true, medications: true, notes: true, created_at: true, doctor: { select: { full_name: true } } }, orderBy: { created_at: "desc" } }); res.json({ success: true, prescriptions: prescriptions.map(({ doctor, ...p }) => ({ ...p, doctor_name: doctor.full_name })) }); } catch (e) { console.error(e); res.status(500).json({ success: false, message: "Internal Server Error" }); } };
const getOwnAppointments = async (req, res) => { try { const appointments = await prisma.appointment.findMany({ where: { patient_id: req.user.id, hospital_id: req.user.hospital_id }, select: { appointment_id: true, appointment_date: true, status: true, doctor: { select: { full_name: true, phone: true } } }, orderBy: { appointment_date: "asc" } }); res.json({ success: true, appointments: appointments.map(({ doctor, ...a }) => ({ ...a, doctor_name: doctor.full_name, doctor_phone: doctor.phone })) }); } catch (e) { console.error(e); res.status(500).json({ success: false, message: "Internal Server Error" }); } };
const getOwnMetrics = async (req, res) => { try { const metrics = await prisma.healthMetric.findMany({ where: { patient_id: req.user.id, hospital_id: req.user.hospital_id }, select: { heart_rate: true, blood_sugar: true, blood_pressure: true, bmi: true, created_at: true }, orderBy: { created_at: "desc" } }); res.json({ success: true, metrics: metrics.map(m => ({ ...m, bmi: m.bmi?.toString() || null })) }); } catch (e) { console.error(e); res.status(500).json({ success: false, message: "Internal Server Error" }); } };
module.exports = { getOwnReports, getOwnPrescriptions, getOwnAppointments, getOwnMetrics };
